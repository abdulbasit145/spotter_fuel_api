services:
  web:
    build:
      context: ../..
      dockerfile: docker/development/Dockerfile
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.development
    volumes:
      - ../..:/app
    ports:
      - "8000:8000"
